# Public site URL (used for sitemap, OG tags, auth callbacks)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Auth.js — optional; demo credentials work without these
AUTH_SECRET=generate-with-openssl-rand-base64-32
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
